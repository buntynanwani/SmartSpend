# ── SmartSpend Environment Variables ─────────────────────────
# Copy this file to .env and fill in your values

# Application
APP_NAME=SmartSpend API
APP_VERSION=1.0.0
DEBUG=True

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=smartspend

# CORS (comma-separated origins)
CORS_ORIGINS=["http://localhost:3000"]
